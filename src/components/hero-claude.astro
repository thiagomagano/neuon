---
// Hero.astro - A hero section component with diagonal split design
---

<section class="hero-container">
  <div class="hero-content">
    <div class="hero-text font-normal space-y-8">
      <h1 class="text-5xl max-w-xl text-white pt-20 leading-14">
        Especialistas em otimizar operações e maximizar lucros através do ERP
        Sankhya.
      </h1>
      <button class="cta-button">Solicite uma demonstração</button>
    </div>
  </div>
</section>

<style>
  .hero-container {
    position: relative;
    height: 800px;
    width: 100%;
    overflow: hidden;
    background-image: url("/images/hero.png");
    background-size: cover;
    background-position: right top;
    background-repeat: no-repeat;
  }

  .hero-container::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 58%;
    height: 100%;
    background-color: #0637ae; /* Royal blue color */
    clip-path: polygon(0 0, 100% 0, 69% 93%, 0 38%);
    z-index: 1;
  }

  .hero-content {
    position: relative;
    height: 100%;
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    z-index: 2;
    display: flex;
    align-items: start;
  }

  .cta-button {
    background-color: #e6a817; /* Gold color for the button */
    color: white;
    border: none;
    padding: 0.75rem 1.5rem;
    font-size: 1rem;
    font-weight: 500;
    cursor: pointer;
    transition: background-color 0.3s ease;
  }

  .cta-button:hover {
    background-color: #d69915;
  }

  @media (max-width: 768px) {
    .hero-container::before {
      width: 100%;
      clip-path: polygon(0 0, 100% 0, 100% 60%, 0 100%);
    }

    .hero-text {
      width: 100%;
      padding: 2rem 1rem;
    }

    .hero-text h1 {
      font-size: 2rem;
    }
  }
</style>
